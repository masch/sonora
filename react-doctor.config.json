{
  "ignore": {
    "overrides": [
      {
        "files": ["src/app/_layout.tsx"],
        "rules": ["deslop/unused-file"],
        "reason": "Expo Router loads _layout.tsx by convention — not an unused file. deslop doesn't understand file-based routing."
      }
    ]
  }
}
