name: 1-PROD Deploy All

on:
  workflow_dispatch:
    inputs:
      build_android:
        description: 'Manual: Compile and deploy Android App'
        required: true
        type: boolean
        default: true
      play_track:
        description: 'Google Play Track (internal, alpha, beta, production)'
        required: true
        type: choice
        options:
          - internal
          - alpha
          - beta
          - production
        default: 'internal'
      enable_firebase:
        description: 'Enable distribution to Firebase App Distribution'
        required: false
        type: boolean
        default: false
      firebase_groups:
        description: 'Firebase App Distribution Groups (comma-separated)'
        required: false
        type: string
        default: 'sonora-team'
  schedule:
    - cron: '0 0 */3 * *'

permissions:
  contents: write

jobs:
  check-changes:
    name: Check for recent commits
    runs-on: ubuntu-latest
    outputs:
      has_changes: ${{ steps.check.outputs.has_changes }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Manual trigger: bypassing change detection."
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          else
            # Check for commits in the last 3 days
            COMMITS=$(git log --since="3 days ago" --oneline)
            if [ -z "$COMMITS" ]; then
              echo "No commits found in the last 3 days."
              echo "has_changes=false" >> "$GITHUB_OUTPUT"
            else
              echo "Commits found:"
              echo "$COMMITS"
              echo "has_changes=true" >> "$GITHUB_OUTPUT"
            fi
          fi

  tag-release:
    name: Tag Release
    needs: check-changes
    if: needs.check-changes.outputs.has_changes == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Tag release
        uses: ./.github/actions/tag-release
        with:
          environment: production

  deploy-admin:
    name: Deploy Admin
    needs: [check-changes, tag-release]
    if: needs.check-changes.outputs.has_changes == 'true'
    uses: ./.github/workflows/deploy-admin-production.yml
    with:
      skip_tagging: true
    secrets: inherit

  deploy-web:
    name: Deploy Web
    needs: [check-changes, deploy-admin, tag-release]
    if: needs.check-changes.outputs.has_changes == 'true'
    uses: ./.github/workflows/deploy-mobile-web-production.yml
    with:
      skip_tagging: true
    secrets: inherit

  deploy-api:
    name: Deploy API
    needs: [check-changes, deploy-web, tag-release]
    if: needs.check-changes.outputs.has_changes == 'true'
    uses: ./.github/workflows/deploy-api-production.yml
    with:
      skip_tagging: true
    secrets: inherit

  deploy-android:
    name: Deploy Android
    needs: [check-changes, deploy-api, tag-release]
    if: |
      always() &&
      needs.check-changes.outputs.has_changes == 'true' &&
      (github.event_name != 'workflow_dispatch' || github.event.inputs.build_android == 'true') &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    uses: ./.github/workflows/deploy-mobile-android-production.yml
    with:
      play_track: ${{ inputs.play_track || github.event.inputs.play_track || 'internal' }}
      enable_firebase: ${{ inputs.enable_firebase == true || github.event.inputs.enable_firebase == true }}
      firebase_groups: ${{ inputs.firebase_groups || github.event.inputs.firebase_groups || 'sonora-team' }}
      skip_tagging: true
    secrets: inherit
