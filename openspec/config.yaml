# ──────────────────────────────────────────────
# OpenSpec Configuration — sonora
# Auto-generated by sdd-init on 2026-05-25
# ──────────────────────────────────────────────

context: >
  Sonora is an Expo SDK 56 universal app (iOS, Android, Web) built with
  Expo Router (file-based routing), React Native 0.85, React 19, TypeScript 6.0
  (strict), and Bun as package manager. Project structure follows
  src/{app,components,constants,hooks} convention with path aliases
  (@/* → src/*, @/assets/* → assets/*). UI layer uses Expo UI primitives,
  CSS variables in global.css, and a theme system via use-color-scheme hooks.
  No backend or data layer yet. AGENTS.md mandates Expo v56 versioned docs.

strict_tdd: true

testing:
  runner: jest
  runner_available:
    - jest
  layers:
    unit: true
    integration: false
    e2e: false
  coverage: false
  coverage_command: null
  linter: make lint
  type_checker: make typecheck
  validate: make validate
  formatter: null

phases:
  explore:
    required: true
  propose:
    required: true
  spec:
    required: true
  design:
    required: true
  tasks:
    required: true
  apply:
    required: true
  verify:
    required: true
  archive:
    required: true
