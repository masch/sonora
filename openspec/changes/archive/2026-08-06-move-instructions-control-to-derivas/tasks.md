# Implementation Tasks

- [x] Remove `HomeAudioPlayer` from `apps/mobile/src/app/(tabs)/index.tsx`.
- [x] Remove `showHomeInstructions` from `@sonora/shared` RemoteConfig schema and defaults.
- [x] Position `HomeAudioPlayer` below the search input and category filter chips in `apps/mobile/src/components/experiences-view.tsx`.
- [x] Unpublish instructions entry in seed data (`apps/api/src/db/seed.ts`).
- [x] Update test suites for `index.test.tsx`, `config.test.ts`, `remote-config-store.test.ts`, and `config-cache.test.ts`.
- [x] Run `make validate` to verify monorepo health and formatting.
