# Tasks: Global Audio Player

- [x] Create the `useCurrentExperience` hook to extract and normalize the active playback state.
- [x] Implement the `GlobalAudioPlayer` component rendering above the tab bar or standalone safe area.
- [x] Implement route-matching hiding logic using `usePathname()`.
- [x] Adjust `ScreenWrapper` and global styles to dynamically handle bottom inset offsets.
- [x] Fix geofence bypass environment comparison bug in `app-config.ts` and set initial fallback config.
- [x] Write Jest unit tests for `useCurrentExperience` and `GlobalAudioPlayer` to verify behavior.
