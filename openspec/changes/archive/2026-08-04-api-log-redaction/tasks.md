# Tasks: API Log Redaction

Change: `api-log-redaction` — Component: `apps/api` (Bun, Hono, Vitest 4.1.10)
Repo root for apply: `/var/home/masch/dev/js/sonora` (R-7: NEVER the session scaffold cwd).
Test commands: focused = `cd apps/api && bun run test <test-path>`; full = `make api-test` (root) → `cd apps/api && bun run test`.
All decisions locked per design (R-A / R-M1 / R-M2 RESOLVED by user 2026-08-04): 7-field body allowlist, absolute query-stripped message URLs, name-only error args + 2 kept internal raw-error paths.

**Delivery decision (user, 2026-08-04): SINGLE PR with explicit size exception** (~700–800 lines, exceeds the 400-line budget by explicit user approval). Chain strategy N/A (no chaining). Apply stages the whole change as ONE PR candidate; Work Units 1–5 land together.

## Review Workload Forecast

| Field                   | Value                                                                                     |
| ----------------------- | ----------------------------------------------------------------------------------------- |
| Estimated changed lines | ~700–800 (additions + deletions)                                                          |
| 400-line budget risk    | High                                                                                      |
| Chained PRs recommended | Yes                                                                                       |
| Suggested split         | PR 1 (helper + tests) → PR 2 (middleware + tests) → PR 3 (payments + http-client + audit) |
| Delivery strategy       | ask-on-risk                                                                               |
| Chain strategy          | pending                                                                                   |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
```

Per-unit line estimates (add/remove): U1 ≈ +265, U2 ≈ +295, U3 ≈ +165, U4 ≈ +155, U5 ≈ 0–10. Each PR slice stays under the 400-line budget; none is independently shippable until PR 1 lands (helper is additive, unused by PR 1 consumers).

## Work Unit 1 — Shared redaction helper (PR 1)

- [x] RED: Create `apps/api/src/lib/__tests__/log-redaction.test.ts` with unit tests for all 4 exports, covering every design §8.1 case: `sanitizeUrl` (absolute w/ query incl. signed sub-URL `?url=https%3A…%3Ftoken%3Dxyz`, query-less unchanged, unparseable garbage → `'<unparseable>'` with raw input absent, relative path stripping, custom scheme `sonora://app/…`, empty string, userinfo stripping), `sanitizeHeaders` (exactly content-type/user-agent/x-request-id, mixed-case input, sensitive absent, empty → `{}`), `extractSafeBodyFields` (spec scenario `{name,email,purchaseId,status,metadata}` → `{purchaseId,status}`; top-level only; nested value at allowlisted key → `'<object>'`/`'<array>'`; array body → `{}`; non-object JSON → `{}`; undefined/null/`{}` no-op; `merchant_order_id`/`externalReference`/`type`/`event`/`providerPaymentId` pass through), `sanitizeQuery` (`{page,limit,sync}` only; `email`, `data.id`, `token` omitted). Run `bun run test src/lib/__tests__/log-redaction.test.ts` — expect module-not-found failure (RED, helper does not exist yet). <!-- sdd-owner: implementation -->
- [x] GREEN: Create `apps/api/src/lib/log-redaction.ts` exporting `HEADER_ALLOWLIST`, `QUERY_ALLOWLIST`, `BODY_FIELD_ALLOWLIST` (locked 7-field set: `purchaseId`, `status`, `event`, `providerPaymentId`, `merchant_order_id`, `externalReference`, `type`), `UNPARSEABLE_URL = '<unparseable>'`, `UNPARSEABLE_BODY = '<unparseable-body>'`, and the 4 functions exactly per design §4 contract (sanitizeUrl origin+path for http(s), scheme+host+path for custom schemes, path-only for leading-`/` strings, fallback constant otherwise; sanitizeHeaders lowercases keys and outputs canonical-lowercase; extractSafeBodyFields top-level only with `<object>`/`<array>` markers; sanitizeQuery allowlist-only). Run focused vitest — green. (~+85) <!-- sdd-owner: implementation -->

## Work Unit 2 — Middleware redesign (PR 2)

- [x] RED: Rewrite `apps/api/src/middleware/__tests__/logger.test.ts` (design §8.2): update the ~~7/8 sensitive-logging assertions — `logs GET requests without body` (response `body` key absent for text/plain, message stays `[API Request] GET http://localhost/test`), `logs POST requests with valid JSON body` (payload `{ name, email, purchaseId: 'uuid-1', status: 'pending' }` → request+response metadata body exactly `{ purchaseId: 'uuid-1', status: 'pending' }`, `name`/`email` absent from all serialized calls), `handles malformed JSON request body gracefully` (`body: '<unparseable-body>'`, raw `{invalid-json` absent from all log calls), `handles non-JSON response bodies` (`body` key absent, client still receives full `<h1>Hello</h1>`), `handles request body read failures gracefully` (URL `/error-body?token=abc`; warn message contains `POST http://localhost/error-body` without query; `token=abc` absent; second arg `{ error: 'Error' }` — NOT `expect.any(Error)`), `handles raw Response objects gracefully` (`body` key absent), `does not disturb the response body stream for a real network client` (KEEP UNCHANGED) plus new sensitive-payload variant route returning `{ checkoutUrl: 'https://pay.example.com/checkout?signed=token123', status: 'approved' }` asserting full body received while `token123`/`checkoutUrl` appear in no log call. KEEP the `ENABLE_API_LOGGING: 'false'` toggle test UNCHANGED. Add negative-invariant tests (design §8.2 list): auth header + cookie values absent from every serialized log call; `/test?deviceId=abc123&email=buyer@example.com` → message `http://localhost/test`, `deviceId=abc123` absent; `/experiences?page=2&limit=10&sync=true` → metadata query `{ page: '2', limit: '10', sync: 'true' }`; `/payments/status/123?email=..&data.id=987&deviceId=dev-1&token=abc` → none appear, message `/payments/status/123`; header-allowlist scenario (authorization/cookie/x-api-key/x-device-id absent; exactly content-type/user-agent/x-request-id present); signed-URL token `signedsecret` absent; form-encoded request → no body metadata; response `{ checkoutUrl, token, providerPaymentId: '123', status: 'approved' }` → body metadata exactly `{ providerPaymentId: '123', status: 'approved' }`. Run `bun run test src/middleware/__tests__/logger.test.ts` — new assertions fail against the current middleware (RED). (~~+200/−120) <!-- sdd-owner: implementation -->
- [x] GREEN: Rewrite `apps/api/src/middleware/logger.ts` per design §5: import `sanitizeUrl`, `sanitizeHeaders`, `sanitizeQuery`, `extractSafeBodyFields`, `UNPARSEABLE_BODY` from `../lib/log-redaction`; keep the `ENABLE_API_LOGGING !== 'false'` early return wrapping ALL buffering (D2); message `[API Request] ${method} ${sanitizeUrl(c.req.url)}`; request metadata `{ headers: sanitizeHeaders(c.req.header()), query: sanitizeQuery(c.req.query()) }` plus `body: extractSafeBodyFields(JSON.parse(...))` only for JSON content-type with `c.req.raw.body`, buffered via `c.req.raw.clone()` (malformed JSON → `UNPARSEABLE_BODY`, never raw text); response side buffers ONLY `application/json` via `c.res.arrayBuffer()` and rebuilds `c.res = new Response(bodyBytes, c.res)` (byte identity preserved regardless of parse outcome; text/binary/HTML/redirect/204 responses never buffered — D3); `[API Response] ${method} ${sanitizedUrl} - ${c.res.status} (${duration}ms)` with `{ status, body? }`; all warn paths embed the sanitized URL and pass name-only `{ error: e instanceof Error ? e.name : 'unknown' }` (D4). Run focused vitest — green. (~+40/−55) <!-- sdd-owner: implementation -->

## Work Unit 3 — payments.ts call-site transformations (PR 3)

- [x] Transform all 9 call sites in `apps/api/src/routes/payments.ts` per design §6 table, importing `sanitizeUrl` from `../lib/log-redaction`: (1) `Creating payment checkout` → `receivedRedirectUrl: redirectUrl ? sanitizeUrl(redirectUrl) : undefined`; (2) webhook `Updating purchase status & preserving metadata` → `existingMetadataPresent`/`incomingMetadataPresent`/`mergedMetadataCount` presence flags only, never the objects; (3) `Return endpoint loaded purchase metadata` → `foundPurchase: !!purchase`, `hasMetadata` flag only, never `purchase?.metadata`; (4) `Failed to parse targetUrl…` → `rawRedirectUrl: sanitizeUrl(meta.redirectUrl)`, `error: error instanceof Error ? error.name : 'unknown'`; (5) `Return endpoint redirecting` → both `rawRedirectUrl` and `finalTargetUrl` through `sanitizeUrl`; (6) `targetUrl resolved to empty…` → `rawRedirectUrl: sanitizeUrl(meta.redirectUrl)`; (7) `Failed to parse Referer header` → `purchaseId`, `status`, `refererOrigin: sanitizeUrl(referer)`, name-only error — never the raw header; (8) `falling back to referer origin` — UNCHANGED (already `url.origin`); (9) `falling back to default callback URL` → `defaultFallbackUrl: sanitizeUrl(defaultFallbackUrl)`. Also apply name-only error args to the two `Failed to parse request URL origin…` warns (consistency, design §6). Leave untouched: duplicate-notification / invalid-transition / missing-external_reference logs, `Active payment status fallback check failed:` and `Failed to read purchase metadata` raw-error paths (R-M2). Run `make api-test` — payments.test.ts behavior spies stay green. (~+40/−25) <!-- sdd-owner: implementation -->

## Work Unit 4 — HttpClient log redaction (PR 3)

- [x] RED: Update `apps/api/src/__tests__/http-client.test.ts`: add top-level `vi.mock('@sonora/shared', () => ({ logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn() } }))`; add negative tests per design §8.3 — request with `Authorization: Bearer outbound-secret` + body `{ email: 'buyer@example.com' }` → serialized log calls contain neither; response text containing `outbound-token-xyz` → response metadata has `status` only, token absent (existing HttpError body-slice assertions on the THROWN error stay valid); error URL `https://provider.example.com/api?client_secret=abc` → error-log message `https://provider.example.com/api`, `client_secret=abc` absent. Run `bun run test src/__tests__/http-client.test.ts` — new assertions fail against the current client (RED). (~+95/−5) <!-- sdd-owner: implementation -->
- [x] GREEN: Apply design §7 to `apps/api/src/lib/http-client.ts` (log-only; fetch call, merged headers, body string, HttpError throw UNTOUCHED): import `sanitizeUrl`/`sanitizeHeaders`; request log → `logger.info('[HTTP Request] ' + method + ' ' + sanitizeUrl(url), { headers: sanitizeHeaders(headers) })` (body NEVER logged); response log → `{ status: res.status }` only, response text NEVER logged; response-text-read warn → name-only `{ error: e instanceof Error ? e.name : 'unknown' }`; error log → query-stripped `sanitizeUrl(url)` message with `{ error: err.name, status: err instanceof HttpError ? err.status : undefined }`. Run focused vitest — green; assert existing behavioral tests (mock fetch, 204, timeout, header merge, HttpError body slice) still pass. (~+30/−25) <!-- sdd-owner: implementation -->

## Work Unit 5 — Invariant sweep, regression gates (PR 3, final)

- [x] Verify `apps/api/src/payments/mercadopago.ts` lines 93–98 (invalid-signature warn: `[METRIC:invalid_signature_total] Invalid signature` with `ts`, `x-request-id`, `data.id`, `reason`) are byte-for-byte unchanged — assert via `git diff --stat`/`git diff apps/api/src/payments/mercadopago.ts` showing no hunks; existing `mercadopago.test.ts` warn assertions (lines ~342+) pass unmodified. (~0) <!-- sdd-owner: implementation -->
- [x] Audit `apps/api/src/__tests__/payments.test.ts` for any payload-dependent log assertion (logger spies are `mockImplementation` no-ops; verified none reference logger output today): if any assertion depends on a logged URL/metadata value, update it to the sanitized form; otherwise leave the file unmodified. (~0–10) <!-- sdd-owner: implementation -->
- [x] Full regression gate: run `make api-test` from `/var/home/masch/dev/js/sonora` AND `make api-typecheck` (api-validate). All suites green: new helper tests, rewritten middleware tests (incl. toggle + real-node-server byte-identity + sensitive-payload variant), http-client negative tests, mercadopago + payments + all other suites untouched and passing. Negative sweep: confirm the serialized log-call markers (`supersecret-token`, `supersecret-session`, `signedsecret`, `deviceId=abc123`, `outbound-secret`, `client_secret=abc`, `buyer@example.com`, `token=abc`) appear in no log call in the new tests. (~0) <!-- sdd-owner: implementation -->

## Post-apply lifecycle (parent-owned)

- [~] Start or reuse bounded review of the frozen candidate (SKIPPED by explicit user decision: "Commit sin review" — receipt-driven-development kill switch was off; user owns disposal status). <!-- sdd-owner: parent -->
