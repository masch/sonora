# Tasks — CI Broccoli PR Approval Gate

Cambio `ci-broccoli-approval`. Fuente: `proposal.md`, `spec.md` (canónico `specs/ci/broccoli-approval/`), `design.md`. Este cambio NO toca código de apps ni `ci-pr.yml`; agrega UN workflow nuevo `.github/workflows/broccoli-approval.yml` + documentación manual de Branch Protection + artefacto OpenSpec.

## Review Workload Forecast

| Field                   | Value                                                 |
| ----------------------- | ----------------------------------------------------- |
| Estimated changed lines | ~180–230 (workflow ~110 + docs ~80–100 + tasks/state) |
| 400-line budget risk    | Low                                                   |
| Chained PRs recommended | No                                                    |
| Suggested split         | single PR                                             |
| Delivery strategy       | single-pr                                             |
| Chain strategy          | pending                                               |

```text
Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: pending
400-line budget risk: Low
```

**Justificación del forecast:** el change está confinado a un archivo nuevo `.github/workflows/broccoli-approval.yml` (~95 líneas), un doc nuevo `docs/broccoli-approval-gate.md` (~80–100 líneas) y el artefacto OpenSpec del cambio. No hay migraciones, tests de unidad, ni código de apps. Rango total bien por debajo de las 400 líneas → **un solo PR**, sin cadena, sin split. Verificación de "test" es estructural (actionlint/YAML parse) + `make format-check`, no TDD de unidad clásico.

## Contexto de verificación

- `strict_tdd: true`, pero este cambio es un workflow YAML sin unidad de test → el método de "test" es **verificación estructural/actionlint** + `format-check`.
- El repo NO declara `actionlint` en su tooling (ni en Makefile ni en `.github/actions`). Si está disponible ad-hoc (`actionlint` o `docker run --rm -v ... rhysd/actionlint`), úselo como validación de sintaxis; si no, use un parse de YAML (ej. `bunx yaml`/`python -c ...`) como fallback de la validación de sintaxis.
- `make format-check` y `make format` corren prettier sobre todo el árbol; mantener el workflow y la doc formateados (prettier aplica a `.yml`/`.md`).
- `on:` del workflow usa `pull_request [opened,synchronize,reopened]` + `issue_comment [created]` + `pull_request_review [submitted]` + `workflow_dispatch`.
- Referencias de estilo: `apps/mobile/.github/workflows/react-doctor.yml` (perms `statuses: write`) y `.github/workflows/security-audit.yml` (`actions/github-script@v8`).
- Documentación de configuración manual va a `docs/` (convenio raíz del repo, ver `README.md` línea 144/161). NO crear `openspec/changes/ci-broccoli-approval/config.md`; la doc de configuración del recurso Branch Protection es un artefacto de infra, no de cambio OpenSpec.

## Tabla de tareas

| id  | task                                                                                                                                    | definition of done / acceptance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | dependencies                                                                                                                                                                                                             | estimated diff                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| T1  | Crear `.github/workflows/broccoli-approval.yml` con el trabajo completo del job `broccoli-gate`.                                        | El archivo existe y cumple TODOS los invariantes de design Section 4/5/6: (1) `name: Broccoli PR Approval Gate`; (2) `on:` con los 4 grupos de eventos exactos; (3) `permissions` EXACTAMENTE `contents: read, issues: write, pull-requests: write, statuses: write` (solo `statuses` añadida vs `ci-pr.yml`); (4) `concurrency.group: broccoli-gate-${{ github.event.pull_request.number \|\| github.event.issue.number }}` y `cancel-in-progress: false`; (5) job único `broccoli-gate` `runs-on: ubuntu-latest` con `if` guard; (6) UN solo step `actions/github-script@v8` implementando: fetch en vivo `pulls.get` (número/base/head), guard `base.ref === 'main'` (si no, return sin escribir status), head SHA SIEMPRE de `prData.head.sha` (nunca base), `synchronize` → `createCommitStatus pending` con context `Check PR broccoli comment`, descripción `Awaiting 🥦 broccoli comment...` y `target_url` del run; evaluación de comentarios (`issue_comment` / `pull_request_review submitted | commented`) →`pulls.get`+`git.getCommit`(committer.date) + paginate`issues.listComments`, filtrar solo comentarios post-head con`>`estricto, tomar el ÚLTIMO,`/\u{1F966}/u`(🥦, with`u`flag) O`/:broccoli:/`, →`success` | `failure`; sin-vaciar último comentario → `failure` (nunca `success` false); el script NUNCA escribe la 🥦. | —   | ~95 líneas |
| T2  | `make format` (o formatear a mano) el YAML para cumplir prettier.                                                                       | `git diff --stat` y `make format-check` pasan (prettier sin cambios pendientes en el workflow).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | T1                                                                                                                                                                                                                       | ~0 (normalización)                                                                                          |
| T3  | Crear `docs/broccoli-approval-gate.md` con la configuración MANUAL del Branch Protection de `main`.                                     | El doc existe y documenta: propósito del gate; pasos en GitHub (Settings → Branches → `main` → Branch Protection → Status checks → REQUIRED exactamente el context `Check PR broccoli comment`, sin espacios, capital C); nota de que este paso es manual y no versionado y que sin él el gate es inerte; cómo verificar (`gh api repos/{owner}/{repo}/commits/{sha}/status` y los 5 pasos de Section 8); permiso mínima y `cancel-in-progress: false`; advertencia de que nunca se escriba el 🥦 automáticamente. Enlazable desde `README.md` solo si se mantiene la sección Documentation (opcional).                                                                                                                                                                                                                                                                                                                                                                                                  | T1 (documenta el gate)                                                                                                                                                                                                   | ~80–100 líneas                                                                                              |
| T4  | Actualizar `openspec/changes/ci-broccoli-approval/` (doc de estado) si el repo lo requiere (mantener `state.yaml`/tracking del cambio). | No rompe `make verify-openspec-archived` (el cambio no está archived de todos modos, es nuevo). No se modifica `ci-pr.yml` ni apps de código.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | —                                                                                                                                                                                                                        | ~0 (tracking)                                                                                               |

## Orden de implementación

1. **T1** → crear el workflow completo (RED de verificación estructural).
2. **T2** → normalizar formato (verde de `make format-check`).
3. **T3** → documentación manual de Branch Protection.
4. **T4** → tracking OpenSpec (si procede).

## Checklist de verificación (post-apply)

Después de implementar y antes de declarar verde/candidate freeze, verificar en este orden:

- [x] **actionlint / YAML syntax**: si `actionlint` está disponible (`actionlint .github/workflows/broccoli-approval.yml`) o, si no, parse YAML del archivo (python/node) sin errores. Sin errores de sintaxis.
- [x] **Formato**: `make format-check` pasa y prettier no deja cambios pendientes en `.github/workflows/broccoli-approval.yml` ni `docs/broccoli-approval-gate.md`.
- [x] **Dry-run / revisión estática**: revisión estructural del YAML según design Section 8: `permissions.statuses` = exactly los CUATRO items (`contents: read, issues: write, pull-requests: write, statuses: write`); `concurrency.cancel-in-progress: false`; los TRES grupos de eventos top-level + `workflow_dispatch`; el string de context constante `Check PR broccoli comment` exact.
- [x] **No-deriva-ci**: NO se modificó `ci-pr.yml` ni apps de app; `openspec/` solo con tracking del cambio nativo.
- [x] **Cobertura del script**: el literal 🥦 NO aparece en el script (se usa `/\u{1F966}/u`), head.sha siempre de `prData.head.sha`, guard `base.ref === 'main'`.
- No hay TDD unitario aplicable (workflow YAML). La verificación de runtime post-merge es liviana, se documenta en T3 (paso en vivo) y corresponde a verify/archive, no a apply.

<!-- owner markers -->

- [x] Implement and verify the YAML workflow behavior (T1–T4). <!-- sdd-owner: implementation -->
- [x] Start or reuse bounded review after apply — SKIPPED by user decision: RDD disabled (gentle-ai review mode off); delivery as disabled/unmanaged, verify SDD structural PASS (8/8 FR). <!-- sdd-owner: parent -->
