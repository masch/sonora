# Tareas: Fondo de pantalla en la Home

- `[ ]` Importar `home-Background.jpeg` en `index.tsx`
- `[ ]` Modificar `ScrollScreenWrapper` y contenedores para usar `bg-transparent`
- `[ ]` Agregar `<TwImage>` posicionada de forma absoluta (`absolute inset-0 w-full h-full`) como primer hijo
- `[ ]` Ejecutar los tests unitarios locales para verificar que no haya regresiones
